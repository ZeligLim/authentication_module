/**
 * HTTP API layer: REST controllers, request/response DTOs, and exception mapping.
 * <p>
 * Controllers delegate to {@code service.*} packages and never access repositories directly.
 */
package com.zelig.authentication_module.api;
